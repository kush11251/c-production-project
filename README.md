# C Production Project
A production-ready C project with separation of concerns across multiple files.

## Build and Run
1. Build the Docker image: `docker build -t c-production-project .`
2. Run the Docker container: `docker run c-production-project`